exports.Input = {
    A: 27,
    // B: 24, // 24 is an invalid pin according to Kaluma
    // (valid if ((pin <= 28) && !((pin == 23) || (pin == 24)))
    B: 22,
    UP: 4,
    DOWN: 6,
    LEFT: 3,
    RIGHT: 5,
};
